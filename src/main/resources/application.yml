spring:
    data:
        mongodb:
            uri: mongodb+srv://vsolabswork:Vs0labs25@cluster0.o5mrlm7.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
            database: daddyJohn
            auto-index-creation: true

    # File upload configuration
    servlet:
        multipart:
            max-file-size: 10MB
            max-request-size: 50MB
            enabled: true

    logging:
        level:
            com.vso.DaddyJohn: DEBUG
            org.springframework.security: DEBUG
            org.springframework.web.cors: DEBUG

server:
    port: ${PORT:8080}

app:
    jwt:
        secret: ${JWT_SECRET:YourDefaultSecretValueIfNotProvided}
        expiration-ms: 3600000

    # File storage configuration
    file:
        upload-dir: ${UPLOAD_DIR:uploads}
        base-url: ${FILE_BASE_URL:http://localhost:8080/files}

services:
    chatbot:
        django-url: https://renderdeploy-1-gpz1.onrender.com/chat